class Solution:
    #Function to check if two arrays are equal or not.
    def check(self,A,B,N):
        
        #return: True or False
        A.sort()
        B.sort()
        if A == B:
            return 1
        return 0
